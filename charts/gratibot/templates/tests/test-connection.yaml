apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gratibot.fullname" . }}-test-connection"
  labels:
{{ include "gratibot.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gratibot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
